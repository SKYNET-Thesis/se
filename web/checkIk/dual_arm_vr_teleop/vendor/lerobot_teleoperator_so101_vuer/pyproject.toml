[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "lerobot_teleoperator_so101_vuer"
version = "0.1.0"
dependencies = [
    "vuer==0.1.6",
    "scipy",
    "robot-descriptions",
    "pyroki @ git+https://github.com/chungmin99/pyroki.git",
]
