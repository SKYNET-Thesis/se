# Domain docs

Use a single context for the whole repository.

Before exploring, read `CONTEXT.md` at the repository root
and relevant decisions in `docs/adr/`.

If these do not exist, proceed silently. The domain-modeling
skill creates them when terminology or decisions are resolved.

Use the vocabulary defined in CONTEXT.md.
Explicitly surface conflicts with existing ADRs.
