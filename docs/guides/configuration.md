# Configuration

Configure explicit stable serial port/device identity, arm side, calibration profile, trusted-LAN host/port, camera configuration, Hub dataset/checkpoint reference and selected SmolVLA checkpoint. Never commit credentials, TLS private keys or hardware identifiers to public evidence.

