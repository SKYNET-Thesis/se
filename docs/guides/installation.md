# Installation

Install dependencies for the selected runtime only. Mobile: `apps/mobile` then `npm install`; control runtime: use the documented LeRobot/Python environment under `web/checkIk`; local services: `docker compose up --build` only for services that are implemented. Do not connect hardware before robot-connection preflight.

