# Calibration

For every newly connected arm: Find Port → Assign correct left/right role → run calibration → verify result → preflight. Failed calibration returns to assigned state and blocks motion.

