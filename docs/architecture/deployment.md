# Deployment

The laptop control host runs the dashboard/control runtime and owns USB serial connections. Phone/Quest use trusted LAN only. Hugging Face Hub stores dataset/checkpoint; Kaggle or rented GPU runs training. Internet exposure of robot control is out of scope.

